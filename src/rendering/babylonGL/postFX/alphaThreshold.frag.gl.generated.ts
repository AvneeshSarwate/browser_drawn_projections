// Auto-generated by glsl fragment generator. DO NOT EDIT.
import * as BABYLON from 'babylonjs';
import { CustomShaderEffect, type ShaderSource, type RenderPrecision, type UniformDescriptor, type ShaderUniforms, type Dynamic } from '../shaderFXBabylon_GL';
export const AlphaThresholdVertexSource = `// Auto-generated by glsl fragment generator. DO NOT EDIT.
#version 300 es
precision highp float;

in vec3 position;
in vec2 uv;
out vec2 vUV;

void main() {
  gl_Position = vec4(position, 1.0);
  vUV = uv;
}`;
export const AlphaThresholdFragmentSources = [
  `// Auto-generated by glsl fragment generator. DO NOT EDIT.
#version 300 es
precision highp float;

in vec2 vUV;
out vec4 outColor;
uniform float uniforms_threshold;
uniform sampler2D src;

struct AlphaThresholdUniforms {
  float threshold;
};

vec4 pass0(vec2 uv, AlphaThresholdUniforms uniforms, sampler2D src) {
  vec4 color = texture(src, uv);
  float newAlpha = color.a > uniforms.threshold ? 1.0 : 0.0;
  return vec4(color.rgb, newAlpha);
}

AlphaThresholdUniforms load_AlphaThresholdUniforms() {
  return AlphaThresholdUniforms(
    uniforms_threshold
  );
}

void main() {
  AlphaThresholdUniforms uniforms_value = load_AlphaThresholdUniforms();
  vec2 uv_local = vUV;
  vec4 color = pass0(uv_local, uniforms_value, src);
  outColor = color;
}`, // pass0
] as const;
export const AlphaThresholdPassCount = 1 as const;
export const AlphaThresholdPrimaryTextureName = 'src' as const;

export const AlphaThresholdPassTextureSources = [
  [
    { binding: 'src', source: { kind: 'input', key: 'src' } },
  ],
] as const;

export const AlphaThresholdUniformMeta: UniformDescriptor[] = [
  {
    name: 'threshold',
    kind: 'f32',
    bindingName: 'uniforms_threshold',
  },
];

export interface AlphaThresholdUniforms {
  threshold: number;
}

export function setAlphaThresholdUniforms(material: BABYLON.ShaderMaterial, uniforms: Partial<AlphaThresholdUniforms>): void {
  if (!uniforms) {
    return;
  }
  if (uniforms.threshold !== undefined) {
    material.setFloat('uniforms_threshold', uniforms.threshold);
  }
}

export type AlphaThresholdTextureName = 'src';
export interface AlphaThresholdInputs {
  src: ShaderSource;
}

export interface AlphaThresholdMaterialHandles {
  material: BABYLON.ShaderMaterial;
  setTexture(name: AlphaThresholdTextureName, texture: BABYLON.BaseTexture): void;
  setTextureSampler(name: AlphaThresholdTextureName, sampler: BABYLON.TextureSampler): void;
  setUniforms(uniforms: Partial<AlphaThresholdUniforms>): void;
}

export interface AlphaThresholdMaterialOptions {
  name?: string;
  passIndex?: number;
}

export function createAlphaThresholdMaterial(scene: BABYLON.Scene, options: AlphaThresholdMaterialOptions = {}): AlphaThresholdMaterialHandles {
  const passIndex = options.passIndex ?? 0;
  if (passIndex < 0 || passIndex >= 1) {
    throw new Error('Invalid passIndex ' + passIndex + ' for AlphaThreshold. Expected 0 <= passIndex < 1.');
  }
  const baseName = options.name ?? 'AlphaThresholdMaterial';
  const shaderName = `${baseName}_pass${passIndex}`;
  const vertexShaderName = `${shaderName}VertexShader`;
  const fragmentShaderName = `${shaderName}FragmentShader`;

  BABYLON.ShaderStore.ShadersStore[vertexShaderName] = AlphaThresholdVertexSource;
  BABYLON.ShaderStore.ShadersStore[fragmentShaderName] = AlphaThresholdFragmentSources[passIndex];

  const material = new BABYLON.ShaderMaterial(shaderName, scene, {
    vertex: shaderName,
    fragment: shaderName,
  }, {
    attributes: ['position', 'uv'],
    uniforms: ['uniforms_threshold'],
    samplers: ['src'],
    shaderLanguage: BABYLON.ShaderLanguage.GLSL,
  });

  const defaultSampler = new BABYLON.TextureSampler();
  defaultSampler.setParameters(
    BABYLON.Texture.CLAMP_ADDRESSMODE,
    BABYLON.Texture.CLAMP_ADDRESSMODE,
    BABYLON.Texture.CLAMP_ADDRESSMODE,
    undefined,
    BABYLON.Texture.BILINEAR_SAMPLINGMODE,
  );
  const samplerState: Record<string, BABYLON.TextureSampler> = {};
  const textureState: Partial<Record<AlphaThresholdTextureName, BABYLON.BaseTexture | null>> = {};
  const applySamplerToTexture = (texture: BABYLON.BaseTexture | null, sampler: BABYLON.TextureSampler) => {
    if (!texture) return;
    const wrapU = sampler.wrapU;
    if (wrapU !== null && wrapU !== undefined) {
      texture.wrapU = wrapU;
    }
    const wrapV = sampler.wrapV;
    if (wrapV !== null && wrapV !== undefined) {
      texture.wrapV = wrapV;
    }
    const wrapR = sampler.wrapR;
    if (wrapR !== null && wrapR !== undefined) {
      texture.wrapR = wrapR;
    }
    texture.updateSamplingMode(sampler.samplingMode);
  };

  const handles: AlphaThresholdMaterialHandles = {
    material,
    setTexture: (name, texture) => {
      material.setTexture(name, texture);
      textureState[name] = texture;
      const sampler = samplerState[name] ?? defaultSampler;
      applySamplerToTexture(texture, sampler);
    },
    setTextureSampler: (name, sampler) => {
      samplerState[name] = sampler;
      const texture = textureState[name] ?? null;
      applySamplerToTexture(texture, sampler);
    },
    setUniforms: (values) => setAlphaThresholdUniforms(material, values),
  };

  return handles;
}

export class AlphaThresholdEffect extends CustomShaderEffect<AlphaThresholdUniforms, AlphaThresholdInputs> {
  effectName = 'AlphaThreshold'

  constructor(engine: BABYLON.Engine, inputs: AlphaThresholdInputs, width = 1280, height = 720, sampleMode: 'nearest' | 'linear' = 'linear', precision: RenderPrecision = 'half_float') {
    super(engine, inputs, {
      factory: (sceneRef, options) => createAlphaThresholdMaterial(sceneRef, options),
      textureInputKeys: ['src'],
      textureBindingKeys: ['src'],
      passTextureSources: AlphaThresholdPassTextureSources,
      passCount: 1,
      primaryTextureKey: 'src',
      width,
      height,
      materialName: 'AlphaThresholdMaterial',
      sampleMode,
      precision,
      uniformMeta: AlphaThresholdUniformMeta,
    });
    this.setUniforms({
    });
  }

  override setSrcs(inputs: Partial<AlphaThresholdInputs>): void {
    super.setSrcs(inputs);
  }

  override setUniforms(uniforms: { threshold?: Dynamic<number> }): void {
    const record: ShaderUniforms = {};
    if (uniforms.threshold !== undefined) {
      record['threshold'] = uniforms.threshold;
    }
    super.setUniforms(record);
  }
}