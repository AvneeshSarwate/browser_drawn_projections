// Auto-generated by glsl fragment generator. DO NOT EDIT.
import * as BABYLON from 'babylonjs';
import { CustomShaderEffect, type ShaderSource, type RenderPrecision, type UniformDescriptor, type ShaderUniforms, type Dynamic } from '../shaderFXBabylon_GL';
export const VerticalBlurVertexSource = `// Auto-generated by glsl fragment generator. DO NOT EDIT.
#version 300 es
precision highp float;

in vec3 position;
in vec2 uv;
out vec2 vUV;

void main() {
  gl_Position = vec4(position, 1.0);
  vUV = uv;
}`;
export const VerticalBlurFragmentSources = [
  `// Auto-generated by glsl fragment generator. DO NOT EDIT.
#version 300 es
precision highp float;

in vec2 vUV;
out vec4 outColor;
uniform int uniforms_pixels;
uniform float uniforms_resolution;
uniform sampler2D src;

struct VerticalBlurUniforms {
  int pixels;
  float resolution;
};

vec4 pass0(vec2 uv, VerticalBlurUniforms uniforms, sampler2D src) {
  if (uniforms.pixels <= 0) {
    return texture(src, uv);
  }

  vec4 color = vec4(0.0);
  float total = 0.0;
  float offset = 1.0 / uniforms.resolution;

  for (int i = -uniforms.pixels; i <= uniforms.pixels; i++) {
    float weight = 1.0 - (abs(float(i)) / float(uniforms.pixels + 1));
    vec2 sampleUV = uv + vec2(0.0, offset * float(i));
    color += texture(src, sampleUV) * weight;
    total += weight;
  }

  return color / total;
}

VerticalBlurUniforms load_VerticalBlurUniforms() {
  return VerticalBlurUniforms(
    uniforms_pixels,
    uniforms_resolution
  );
}

void main() {
  VerticalBlurUniforms uniforms_value = load_VerticalBlurUniforms();
  vec2 uv_local = vUV;
  vec4 color = pass0(uv_local, uniforms_value, src);
  outColor = color;
}`, // pass0
] as const;
export const VerticalBlurPassCount = 1 as const;
export const VerticalBlurPrimaryTextureName = 'src' as const;

export const VerticalBlurPassTextureSources = [
  [
    { binding: 'src', source: { kind: 'input', key: 'src' } },
  ],
] as const;

export const VerticalBlurUniformMeta: UniformDescriptor[] = [
  {
    name: 'pixels',
    kind: 'i32',
    bindingName: 'uniforms_pixels',
  },
  {
    name: 'resolution',
    kind: 'f32',
    bindingName: 'uniforms_resolution',
  },
];

export interface VerticalBlurUniforms {
  pixels: number;
  resolution: number;
}

export function setVerticalBlurUniforms(material: BABYLON.ShaderMaterial, uniforms: Partial<VerticalBlurUniforms>): void {
  if (!uniforms) {
    return;
  }
  if (uniforms.pixels !== undefined) {
    material.setInt('uniforms_pixels', uniforms.pixels);
  }
  if (uniforms.resolution !== undefined) {
    material.setFloat('uniforms_resolution', uniforms.resolution);
  }
}

export type VerticalBlurTextureName = 'src';
export interface VerticalBlurInputs {
  src: ShaderSource;
}

export interface VerticalBlurMaterialHandles {
  material: BABYLON.ShaderMaterial;
  setTexture(name: VerticalBlurTextureName, texture: BABYLON.BaseTexture): void;
  setTextureSampler(name: VerticalBlurTextureName, sampler: BABYLON.TextureSampler): void;
  setUniforms(uniforms: Partial<VerticalBlurUniforms>): void;
}

export interface VerticalBlurMaterialOptions {
  name?: string;
  passIndex?: number;
}

export function createVerticalBlurMaterial(scene: BABYLON.Scene, options: VerticalBlurMaterialOptions = {}): VerticalBlurMaterialHandles {
  const passIndex = options.passIndex ?? 0;
  if (passIndex < 0 || passIndex >= 1) {
    throw new Error('Invalid passIndex ' + passIndex + ' for VerticalBlur. Expected 0 <= passIndex < 1.');
  }
  const baseName = options.name ?? 'VerticalBlurMaterial';
  const shaderName = `${baseName}_pass${passIndex}`;
  const vertexShaderName = `${shaderName}VertexShader`;
  const fragmentShaderName = `${shaderName}FragmentShader`;

  BABYLON.ShaderStore.ShadersStore[vertexShaderName] = VerticalBlurVertexSource;
  BABYLON.ShaderStore.ShadersStore[fragmentShaderName] = VerticalBlurFragmentSources[passIndex];

  const material = new BABYLON.ShaderMaterial(shaderName, scene, {
    vertex: shaderName,
    fragment: shaderName,
  }, {
    attributes: ['position', 'uv'],
    uniforms: ['uniforms_pixels', 'uniforms_resolution'],
    samplers: ['src'],
    shaderLanguage: BABYLON.ShaderLanguage.GLSL,
  });

  const defaultSampler = new BABYLON.TextureSampler();
  defaultSampler.setParameters(
    BABYLON.Texture.CLAMP_ADDRESSMODE,
    BABYLON.Texture.CLAMP_ADDRESSMODE,
    BABYLON.Texture.CLAMP_ADDRESSMODE,
    undefined,
    BABYLON.Texture.BILINEAR_SAMPLINGMODE,
  );
  const samplerState: Record<string, BABYLON.TextureSampler> = {};
  const textureState: Partial<Record<VerticalBlurTextureName, BABYLON.BaseTexture | null>> = {};
  const applySamplerToTexture = (texture: BABYLON.BaseTexture | null, sampler: BABYLON.TextureSampler) => {
    if (!texture) return;
    const wrapU = sampler.wrapU;
    if (wrapU !== null && wrapU !== undefined) {
      texture.wrapU = wrapU;
    }
    const wrapV = sampler.wrapV;
    if (wrapV !== null && wrapV !== undefined) {
      texture.wrapV = wrapV;
    }
    const wrapR = sampler.wrapR;
    if (wrapR !== null && wrapR !== undefined) {
      texture.wrapR = wrapR;
    }
    texture.updateSamplingMode(sampler.samplingMode);
  };

  const handles: VerticalBlurMaterialHandles = {
    material,
    setTexture: (name, texture) => {
      material.setTexture(name, texture);
      textureState[name] = texture;
      const sampler = samplerState[name] ?? defaultSampler;
      applySamplerToTexture(texture, sampler);
    },
    setTextureSampler: (name, sampler) => {
      samplerState[name] = sampler;
      const texture = textureState[name] ?? null;
      applySamplerToTexture(texture, sampler);
    },
    setUniforms: (values) => setVerticalBlurUniforms(material, values),
  };

  return handles;
}

export class VerticalBlurEffect extends CustomShaderEffect<VerticalBlurUniforms, VerticalBlurInputs> {
  effectName = 'VerticalBlur'

  constructor(engine: BABYLON.Engine, inputs: VerticalBlurInputs, width = 1280, height = 720, sampleMode: 'nearest' | 'linear' = 'linear', precision: RenderPrecision = 'half_float') {
    super(engine, inputs, {
      factory: (sceneRef, options) => createVerticalBlurMaterial(sceneRef, options),
      textureInputKeys: ['src'],
      textureBindingKeys: ['src'],
      passTextureSources: VerticalBlurPassTextureSources,
      passCount: 1,
      primaryTextureKey: 'src',
      width,
      height,
      materialName: 'VerticalBlurMaterial',
      sampleMode,
      precision,
      uniformMeta: VerticalBlurUniformMeta,
    });
    this.setUniforms({
    });
  }

  override setSrcs(inputs: Partial<VerticalBlurInputs>): void {
    super.setSrcs(inputs);
  }

  override setUniforms(uniforms: { pixels?: Dynamic<number>; resolution?: Dynamic<number> }): void {
    const record: ShaderUniforms = {};
    if (uniforms.pixels !== undefined) {
      record['pixels'] = uniforms.pixels;
    }
    if (uniforms.resolution !== undefined) {
      record['resolution'] = uniforms.resolution;
    }
    super.setUniforms(record);
  }
}