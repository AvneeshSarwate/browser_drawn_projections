// Auto-generated by glsl fragment generator. DO NOT EDIT.
import * as BABYLON from 'babylonjs';
import { CustomShaderEffect, type ShaderSource, type RenderPrecision, type UniformDescriptor, type ShaderUniforms, type Dynamic } from '../shaderFXBabylon_GL';
export const WobbleVertexSource = `// Auto-generated by glsl fragment generator. DO NOT EDIT.
#version 300 es
precision highp float;

in vec3 position;
in vec2 uv;
out vec2 vUV;

void main() {
  gl_Position = vec4(position, 1.0);
  vUV = uv;
}`;
export const WobbleFragmentSources = [
  `// Auto-generated by glsl fragment generator. DO NOT EDIT.
#version 300 es
precision highp float;

in vec2 vUV;
out vec4 outColor;
uniform float uniforms_xStrength;
uniform float uniforms_yStrength;
uniform float uniforms_time;
uniform sampler2D src;

struct WobbleUniforms {
  float xStrength;
  float yStrength;
  float time;
};

vec4 pass0(vec2 uv, WobbleUniforms uniforms, sampler2D src) {
  vec2 uv2 = uv;
  uv2.x = uv2.x + sin(uv.y * 10.0 + uniforms.time * 2.0) * uniforms.xStrength;
  uv2.y = uv2.y + cos(uv.x * 10.0 + uniforms.time * 2.0) * uniforms.yStrength;
  return texture(src, uv2);
}

WobbleUniforms load_WobbleUniforms() {
  return WobbleUniforms(
    uniforms_xStrength,
    uniforms_yStrength,
    uniforms_time
  );
}

void main() {
  WobbleUniforms uniforms_value = load_WobbleUniforms();
  vec2 uv_local = vUV;
  vec4 color = pass0(uv_local, uniforms_value, src);
  outColor = color;
}`, // pass0
] as const;
export const WobblePassCount = 1 as const;
export const WobblePrimaryTextureName = 'src' as const;

export const WobblePassTextureSources = [
  [
    { binding: 'src', source: { kind: 'input', key: 'src' } },
  ],
] as const;

export const WobbleUniformMeta: UniformDescriptor[] = [
  {
    name: 'xStrength',
    kind: 'f32',
    bindingName: 'uniforms_xStrength',
  },
  {
    name: 'yStrength',
    kind: 'f32',
    bindingName: 'uniforms_yStrength',
  },
  {
    name: 'time',
    kind: 'f32',
    bindingName: 'uniforms_time',
  },
];

export interface WobbleUniforms {
  xStrength: number;
  yStrength: number;
  time: number;
}

export function setWobbleUniforms(material: BABYLON.ShaderMaterial, uniforms: Partial<WobbleUniforms>): void {
  if (!uniforms) {
    return;
  }
  if (uniforms.xStrength !== undefined) {
    material.setFloat('uniforms_xStrength', uniforms.xStrength);
  }
  if (uniforms.yStrength !== undefined) {
    material.setFloat('uniforms_yStrength', uniforms.yStrength);
  }
  if (uniforms.time !== undefined) {
    material.setFloat('uniforms_time', uniforms.time);
  }
}

export type WobbleTextureName = 'src';
export interface WobbleInputs {
  src: ShaderSource;
}

export interface WobbleMaterialHandles {
  material: BABYLON.ShaderMaterial;
  setTexture(name: WobbleTextureName, texture: BABYLON.BaseTexture): void;
  setTextureSampler(name: WobbleTextureName, sampler: BABYLON.TextureSampler): void;
  setUniforms(uniforms: Partial<WobbleUniforms>): void;
}

export interface WobbleMaterialOptions {
  name?: string;
  passIndex?: number;
}

export function createWobbleMaterial(scene: BABYLON.Scene, options: WobbleMaterialOptions = {}): WobbleMaterialHandles {
  const passIndex = options.passIndex ?? 0;
  if (passIndex < 0 || passIndex >= 1) {
    throw new Error('Invalid passIndex ' + passIndex + ' for Wobble. Expected 0 <= passIndex < 1.');
  }
  const baseName = options.name ?? 'WobbleMaterial';
  const shaderName = `${baseName}_pass${passIndex}`;
  const vertexShaderName = `${shaderName}VertexShader`;
  const fragmentShaderName = `${shaderName}FragmentShader`;

  BABYLON.ShaderStore.ShadersStore[vertexShaderName] = WobbleVertexSource;
  BABYLON.ShaderStore.ShadersStore[fragmentShaderName] = WobbleFragmentSources[passIndex];

  const material = new BABYLON.ShaderMaterial(shaderName, scene, {
    vertex: shaderName,
    fragment: shaderName,
  }, {
    attributes: ['position', 'uv'],
    uniforms: ['uniforms_xStrength', 'uniforms_yStrength', 'uniforms_time'],
    samplers: ['src'],
    shaderLanguage: BABYLON.ShaderLanguage.GLSL,
  });

  const defaultSampler = new BABYLON.TextureSampler();
  defaultSampler.setParameters(
    BABYLON.Texture.CLAMP_ADDRESSMODE,
    BABYLON.Texture.CLAMP_ADDRESSMODE,
    BABYLON.Texture.CLAMP_ADDRESSMODE,
    undefined,
    BABYLON.Texture.BILINEAR_SAMPLINGMODE,
  );
  const samplerState: Record<string, BABYLON.TextureSampler> = {};
  const textureState: Partial<Record<WobbleTextureName, BABYLON.BaseTexture | null>> = {};
  const applySamplerToTexture = (texture: BABYLON.BaseTexture | null, sampler: BABYLON.TextureSampler) => {
    if (!texture) return;
    const wrapU = sampler.wrapU;
    if (wrapU !== null && wrapU !== undefined) {
      texture.wrapU = wrapU;
    }
    const wrapV = sampler.wrapV;
    if (wrapV !== null && wrapV !== undefined) {
      texture.wrapV = wrapV;
    }
    const wrapR = sampler.wrapR;
    if (wrapR !== null && wrapR !== undefined) {
      texture.wrapR = wrapR;
    }
    texture.updateSamplingMode(sampler.samplingMode);
  };

  const handles: WobbleMaterialHandles = {
    material,
    setTexture: (name, texture) => {
      material.setTexture(name, texture);
      textureState[name] = texture;
      const sampler = samplerState[name] ?? defaultSampler;
      applySamplerToTexture(texture, sampler);
    },
    setTextureSampler: (name, sampler) => {
      samplerState[name] = sampler;
      const texture = textureState[name] ?? null;
      applySamplerToTexture(texture, sampler);
    },
    setUniforms: (values) => setWobbleUniforms(material, values),
  };

  return handles;
}

export class WobbleEffect extends CustomShaderEffect<WobbleUniforms, WobbleInputs> {
  effectName = 'Wobble'

  constructor(engine: BABYLON.Engine, inputs: WobbleInputs, width = 1280, height = 720, sampleMode: 'nearest' | 'linear' = 'linear', precision: RenderPrecision = 'half_float') {
    super(engine, inputs, {
      factory: (sceneRef, options) => createWobbleMaterial(sceneRef, options),
      textureInputKeys: ['src'],
      textureBindingKeys: ['src'],
      passTextureSources: WobblePassTextureSources,
      passCount: 1,
      primaryTextureKey: 'src',
      width,
      height,
      materialName: 'WobbleMaterial',
      sampleMode,
      precision,
      uniformMeta: WobbleUniformMeta,
    });
    this.setUniforms({
    });
  }

  override setSrcs(inputs: Partial<WobbleInputs>): void {
    super.setSrcs(inputs);
  }

  override setUniforms(uniforms: { xStrength?: Dynamic<number>; yStrength?: Dynamic<number>; time?: Dynamic<number> }): void {
    const record: ShaderUniforms = {};
    if (uniforms.xStrength !== undefined) {
      record['xStrength'] = uniforms.xStrength;
    }
    if (uniforms.yStrength !== undefined) {
      record['yStrength'] = uniforms.yStrength;
    }
    if (uniforms.time !== undefined) {
      record['time'] = uniforms.time;
    }
    super.setUniforms(record);
  }
}