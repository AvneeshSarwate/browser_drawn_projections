// Auto-generated by wgsl fragment generator. DO NOT EDIT.
import * as BABYLON from 'babylonjs';
import { CustomShaderEffect, type ShaderSource, type RenderPrecision, type UniformDescriptor, type ShaderUniforms, type Dynamic } from '../shaderFXBabylon';
export const SplatVertexSource = `// Auto-generated by wgsl fragment generator. DO NOT EDIT.
attribute position: vec3<f32>;
attribute uv: vec2<f32>;
varying vUV: vec2<f32>;

#define CUSTOM_VERTEX_DEFINITIONS
@vertex
fn main(input : VertexInputs) -> FragmentInputs {
#define CUSTOM_VERTEX_MAIN_BEGIN
  vertexOutputs.position = vec4<f32>(vertexInputs.position, 1.0);
  vertexOutputs.vUV = vertexInputs.uv;
#define CUSTOM_VERTEX_MAIN_END
}

`;
export const SplatFragmentSources = [
  `// Auto-generated by wgsl fragment generator. DO NOT EDIT.
varying vUV: vec2<f32>;
uniform uniforms_point: vec2f;
uniform uniforms_color: vec3f;
uniform uniforms_radius: f32;
uniform uniforms_aspectRatio: f32;
var splatTarget: texture_2d<f32>;
var splatTargetSampler: sampler;

// Source fragment function
struct SplatUniforms {
  point: vec2<f32>,
  color: vec3<f32>,
  radius: f32,
  aspectRatio: f32,
}

fn pass0(
  uv: vec2f,
  uniforms: SplatUniforms,
  splatTarget: texture_2d<f32>,
  splatTargetSampler: sampler,
) -> vec4f {
  let clampedUv = clamp(uv, vec2f(0.0), vec2f(1.0));
  let base = textureSample(splatTarget, splatTargetSampler, clampedUv);

  var offset = clampedUv - uniforms.point;
  offset.x = offset.x * uniforms.aspectRatio;

  let influence = exp(-dot(offset, offset) / max(uniforms.radius, 1e-6));
  let added = uniforms.color * influence;
  return vec4f(base.xyz + added, base.w);
}

fn load_SplatUniforms() -> SplatUniforms {
  var result: SplatUniforms;
  result.point = uniforms.uniforms_point;
  result.color = uniforms.uniforms_color;
  result.radius = uniforms.uniforms_radius;
  result.aspectRatio = uniforms.uniforms_aspectRatio;
  return result;
}

#define CUSTOM_FRAGMENT_DEFINITIONS
@fragment
fn main(input: FragmentInputs) -> FragmentOutputs {
#define CUSTOM_FRAGMENT_MAIN_BEGIN
  let uniforms_value = load_SplatUniforms();
  let uv_local = fragmentInputs.vUV;
  let color = pass0(uv_local, uniforms_value, splatTarget, splatTargetSampler);
  fragmentOutputs.color = color;
#define CUSTOM_FRAGMENT_MAIN_END
}

`, // pass0
] as const;
export const SplatPassCount = 1 as const;
export const SplatPrimaryTextureName = 'splatTarget' as const;

export const SplatPassTextureSources = [
  [
    { binding: 'splatTarget', source: { kind: 'input', key: 'splatTarget' } },
  ],
] as const;

export const SplatUniformMeta: UniformDescriptor[] = [
  {
    name: 'point',
    kind: 'vec2f',
    bindingName: 'uniforms_point',
  },
  {
    name: 'color',
    kind: 'vec3f',
    bindingName: 'uniforms_color',
  },
  {
    name: 'radius',
    kind: 'f32',
    bindingName: 'uniforms_radius',
  },
  {
    name: 'aspectRatio',
    kind: 'f32',
    bindingName: 'uniforms_aspectRatio',
  },
];

function ensureVector2(value: BABYLON.Vector2 | readonly [number, number]): BABYLON.Vector2 {
  return value instanceof BABYLON.Vector2 ? value : BABYLON.Vector2.FromArray(value as readonly [number, number]);
}
function ensureVector3(value: BABYLON.Vector3 | readonly [number, number, number]): BABYLON.Vector3 {
  return value instanceof BABYLON.Vector3 ? value : BABYLON.Vector3.FromArray(value as readonly [number, number, number]);
}

export interface SplatUniforms {
  point: BABYLON.Vector2 | readonly [number, number];
  color: BABYLON.Vector3 | readonly [number, number, number];
  radius: number;
  aspectRatio: number;
}

export function setSplatUniforms(material: BABYLON.ShaderMaterial, uniforms: Partial<SplatUniforms>): void {
  if (!uniforms) {
    return;
  }
  if (uniforms.point !== undefined) {
    material.setVector2('uniforms_point', ensureVector2(uniforms.point));
  }
  if (uniforms.color !== undefined) {
    material.setVector3('uniforms_color', ensureVector3(uniforms.color));
  }
  if (uniforms.radius !== undefined) {
    material.setFloat('uniforms_radius', uniforms.radius);
  }
  if (uniforms.aspectRatio !== undefined) {
    material.setFloat('uniforms_aspectRatio', uniforms.aspectRatio);
  }
}

export type SplatTextureName = 'splatTarget';
export interface SplatInputs {
  splatTarget: ShaderSource;
}

export interface SplatMaterialHandles {
  material: BABYLON.ShaderMaterial;
  setTexture(name: SplatTextureName, texture: BABYLON.BaseTexture): void;
  setTextureSampler(name: SplatTextureName, sampler: BABYLON.TextureSampler): void;
  setUniforms(uniforms: Partial<SplatUniforms>): void;
}

export interface SplatMaterialOptions {
  name?: string;
  passIndex?: number;
}

export function createSplatMaterial(scene: BABYLON.Scene, options: SplatMaterialOptions = {}): SplatMaterialHandles {
  const passIndex = options.passIndex ?? 0;
  if (passIndex < 0 || passIndex >= 1) {
    throw new Error(`Invalid passIndex ${passIndex} for Splat. Expected 0 <= passIndex < 1.`);
  }
  const baseName = options.name ?? 'SplatMaterial';
  const shaderName = `${baseName}_pass${passIndex}`;
  // Register shaders in the WGSL store to enable preprocessor
  const vertexShaderName = `${shaderName}VertexShader`;
  const fragmentShaderName = `${shaderName}FragmentShader`;
  
  BABYLON.ShaderStore.ShadersStoreWGSL[vertexShaderName] = SplatVertexSource;
  BABYLON.ShaderStore.ShadersStoreWGSL[fragmentShaderName] = SplatFragmentSources[passIndex];
  
  const material = new BABYLON.ShaderMaterial(shaderName, scene, {
    vertex: shaderName,
    fragment: shaderName,
  }, {
    attributes: ['position', 'uv'],
    uniforms: ['uniforms_point', 'uniforms_color', 'uniforms_radius', 'uniforms_aspectRatio'],
    samplers: ['splatTarget'],
    samplerObjects: ['splatTargetSampler'],
    shaderLanguage: BABYLON.ShaderLanguage.WGSL,
  });

  const samplerLookup = { 'splatTarget': 'splatTargetSampler' } as const;

  const handles: SplatMaterialHandles = {
    material,
    setTexture: (name, texture) => material.setTexture(name, texture),
    setTextureSampler: (name, sampler) => material.setTextureSampler(samplerLookup[name], sampler),
    setUniforms: (values) => setSplatUniforms(material, values),
  };

  return handles;
}

export class SplatEffect extends CustomShaderEffect<SplatUniforms, SplatInputs> {
  effectName = 'Splat'

  constructor(engine: BABYLON.WebGPUEngine, inputs: SplatInputs, width = 1280, height = 720, sampleMode: 'nearest' | 'linear' = 'linear', precision: RenderPrecision = 'half_float') {
    super(engine, inputs, {
      factory: (sceneRef, options) => createSplatMaterial(sceneRef, options),
      textureInputKeys: ['splatTarget'],
      textureBindingKeys: ['splatTarget'],
      passTextureSources: SplatPassTextureSources,
      passCount: 1,
      primaryTextureKey: 'splatTarget',
      width,
      height,
      materialName: 'SplatMaterial',
      sampleMode,
      precision,
      uniformMeta: SplatUniformMeta,
    })
  }

  override setSrcs(inputs: Partial<SplatInputs>): void {
    super.setSrcs(inputs);
  }

  override setUniforms(uniforms: { point?: Dynamic<BABYLON.Vector2 | readonly [number, number]>, color?: Dynamic<BABYLON.Vector3 | readonly [number, number, number]>, radius?: Dynamic<number>, aspectRatio?: Dynamic<number> }): void {
    const record: ShaderUniforms = {};
    if (uniforms.point !== undefined) {
      record['point'] = uniforms.point;
    }
    if (uniforms.color !== undefined) {
      record['color'] = uniforms.color;
    }
    if (uniforms.radius !== undefined) {
      record['radius'] = uniforms.radius;
    }
    if (uniforms.aspectRatio !== undefined) {
      record['aspectRatio'] = uniforms.aspectRatio;
    }
    super.setUniforms(record);
  }
}

