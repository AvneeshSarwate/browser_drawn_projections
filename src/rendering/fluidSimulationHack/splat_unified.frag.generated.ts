// Auto-generated by wgsl fragment generator. DO NOT EDIT.
import * as BABYLON from 'babylonjs';
import { CustomShaderEffect, type ShaderSource, type RenderPrecision, type UniformDescriptor, type ShaderUniforms, type Dynamic } from '../shaderFXBabylon';
export const SplatUnifiedVertexSource = `// Auto-generated by wgsl fragment generator. DO NOT EDIT.
attribute position: vec3<f32>;
attribute uv: vec2<f32>;
varying vUV: vec2<f32>;

#define CUSTOM_VERTEX_DEFINITIONS
@vertex
fn main(input : VertexInputs) -> FragmentInputs {
#define CUSTOM_VERTEX_MAIN_BEGIN
  vertexOutputs.position = vec4<f32>(vertexInputs.position, 1.0);
  vertexOutputs.vUV = vertexInputs.uv;
#define CUSTOM_VERTEX_MAIN_END
}

`;
export const SplatUnifiedFragmentSources = [
  `// Auto-generated by wgsl fragment generator. DO NOT EDIT.
varying vUV: vec2<f32>;
uniform uniforms_mode: u32;
uniform uniforms_splatType: u32;
uniform uniforms_point: vec2f;
uniform uniforms_color: vec3f;
uniform uniforms_radius: f32;
uniform uniforms_aspectRatio: f32;
uniform uniforms_strength: f32;
uniform uniforms_time: f32;
uniform uniforms_shapeParams0: vec4f;
uniform uniforms_shapeParams1: vec4f;
var base: texture_2d<f32>;
var baseSampler: sampler;

// Source fragment function
struct SplatUniforms {
  mode: u32,
  splatType: u32,
  point: vec2<f32>,
  color: vec3<f32>,
  radius: f32,
  aspectRatio: f32,
  strength: f32,
  time: f32,
  shapeParams0: vec4<f32>,
  shapeParams1: vec4<f32>,
}

fn gaussianSplat(uv: vec2f, uniforms: SplatUniforms) -> vec4f {
  var offset = uv - uniforms.point;
  offset.x = offset.x * uniforms.aspectRatio;
  let sigma = max(uniforms.radius, 1e-6);
  let influence = exp(-dot(offset, offset) / (2.0 * sigma * sigma));
  let delta = uniforms.color * influence;
  return vec4f(delta, influence);
}

fn rectangleSplat(uv: vec2f, uniforms: SplatUniforms) -> vec4f {
  let halfSize = max(uniforms.shapeParams0.xy, vec2f(1e-4, 1e-4));
  let baseAngle = uniforms.shapeParams0.z;
  let rotationSpeed = uniforms.shapeParams1.x;
  let pulseAmount = uniforms.shapeParams1.y;
  let pulseFrequency = uniforms.shapeParams1.z;
  let proceduralForce = uniforms.shapeParams1.w;
  let softness = max(uniforms.shapeParams0.w, 1e-3);
  let rotation = baseAngle + rotationSpeed * uniforms.time;
  let dynamicScale = 1.0 + pulseAmount * sin(uniforms.time * pulseFrequency);

  let offset = uv - uniforms.point;
  var local = offset;
  local.x = local.x * uniforms.aspectRatio;

  let cosTheta = cos(rotation);
  let sinTheta = sin(rotation);
  let rotated = vec2f(
    cosTheta * local.x + sinTheta * local.y,
    -sinTheta * local.x + cosTheta * local.y,
  );

  let effectiveHalfSize = halfSize * max(dynamicScale, 1e-3);
  let normalized = vec2f(rotated.x / effectiveHalfSize.x, rotated.y / effectiveHalfSize.y);
  let distance = max(abs(normalized.x), abs(normalized.y));
  let mask = 1.0 - smoothstep(1.0, 1.0 + softness, distance);

  let col = vec3f(0.0, 0.0, 0.0);
  var delta = vec3f(uniforms.color * mask);

  if (abs(proceduralForce) > 1e-5) {
    let tangential = vec2f(-offset.y, offset.x);
    let tangentialLength = length(tangential);
    if (tangentialLength > 1e-5) {
      let normalizedTangential = tangential / tangentialLength;
      let radiusFactor = clamp(length(normalized), 0.0, 1.0);
      let velocityMagnitude = proceduralForce * radiusFactor * mask;
      let velocityDelta = normalizedTangential * velocityMagnitude;
      delta = vec3f(delta.xy + velocityDelta, delta.z);
    }
  }

  return vec4f(delta, mask);
}

fn computeSplat(uv: vec2f, uniforms: SplatUniforms) -> vec4f {
  switch uniforms.splatType {
    case 0u {
      return gaussianSplat(uv, uniforms);
    }
    case 1u {
      return rectangleSplat(uv, uniforms);
    }
    default {
      return gaussianSplat(uv, uniforms);
    }
  }
}

// Pass 0: Compute the impulse delta for the selected procedural splat
// This is a pure function that computes the splat without reading base
fn pass0(
  uv: vec2f,
  uniforms: SplatUniforms,
  base: texture_2d<f32>,
  baseSampler: sampler,
) -> vec4f {
  let clampedUv = clamp(uv, vec2f(0.0), vec2f(1.0));
  return computeSplat(clampedUv, uniforms);
}

// Pass 1: Apply the delta to the base based on mode
fn pass1(
  uv: vec2f,
  uniforms: SplatUniforms,
  base: texture_2d<f32>,
  baseSampler: sampler,
  pass0Texture: texture_2d<f32>,
  pass0Sampler: sampler,
) -> vec4f {
  let clampedUv = clamp(uv, vec2f(0.0), vec2f(1.0));
  let baseColor = textureSample(base, baseSampler, clampedUv);
  let delta = textureSample(pass0Texture, pass0Sampler, clampedUv);
  
  // Mode-based behavior
  if (uniforms.mode == 0u) {
    // Passthrough mode: just return base
    return baseColor;
  } else if (uniforms.mode == 1u) {
    // Additive mode: add delta * strength to base
    return vec4f(baseColor.xyz + delta.xyz * uniforms.strength, 1.0);
  } else {
    // Reserved for exotic operations (mode >= 2)
    return baseColor;
  }
}

fn load_SplatUniforms() -> SplatUniforms {
  return SplatUniforms(
  uniforms.uniforms_mode,
  uniforms.uniforms_splatType,
  uniforms.uniforms_point,
  uniforms.uniforms_color,
  uniforms.uniforms_radius,
  uniforms.uniforms_aspectRatio,
  uniforms.uniforms_strength,
  uniforms.uniforms_time,
  uniforms.uniforms_shapeParams0,
  uniforms.uniforms_shapeParams1
  );
}

#define CUSTOM_FRAGMENT_DEFINITIONS
@fragment
fn main(input: FragmentInputs) -> FragmentOutputs {
#define CUSTOM_FRAGMENT_MAIN_BEGIN
  let uniforms_value = load_SplatUniforms();
  let uv_local = fragmentInputs.vUV;
  let color = pass0(uv_local, uniforms_value, base, baseSampler);
  fragmentOutputs.color = color;
#define CUSTOM_FRAGMENT_MAIN_END
}

`, // pass0
  `// Auto-generated by wgsl fragment generator. DO NOT EDIT.
varying vUV: vec2<f32>;
uniform uniforms_mode: u32;
uniform uniforms_splatType: u32;
uniform uniforms_point: vec2f;
uniform uniforms_color: vec3f;
uniform uniforms_radius: f32;
uniform uniforms_aspectRatio: f32;
uniform uniforms_strength: f32;
uniform uniforms_time: f32;
uniform uniforms_shapeParams0: vec4f;
uniform uniforms_shapeParams1: vec4f;
var base: texture_2d<f32>;
var baseSampler: sampler;
var pass0Texture: texture_2d<f32>;
var pass0Sampler: sampler;

// Source fragment function
struct SplatUniforms {
  mode: u32,
  splatType: u32,
  point: vec2<f32>,
  color: vec3<f32>,
  radius: f32,
  aspectRatio: f32,
  strength: f32,
  time: f32,
  shapeParams0: vec4<f32>,
  shapeParams1: vec4<f32>,
}

fn gaussianSplat(uv: vec2f, uniforms: SplatUniforms) -> vec4f {
  var offset = uv - uniforms.point;
  offset.x = offset.x * uniforms.aspectRatio;
  let sigma = max(uniforms.radius, 1e-6);
  let influence = exp(-dot(offset, offset) / (2.0 * sigma * sigma));
  let delta = uniforms.color * influence;
  return vec4f(delta, influence);
}

fn rectangleSplat(uv: vec2f, uniforms: SplatUniforms) -> vec4f {
  let halfSize = max(uniforms.shapeParams0.xy, vec2f(1e-4, 1e-4));
  let baseAngle = uniforms.shapeParams0.z;
  let rotationSpeed = uniforms.shapeParams1.x;
  let pulseAmount = uniforms.shapeParams1.y;
  let pulseFrequency = uniforms.shapeParams1.z;
  let proceduralForce = uniforms.shapeParams1.w;
  let softness = max(uniforms.shapeParams0.w, 1e-3);
  let rotation = baseAngle + rotationSpeed * uniforms.time;
  let dynamicScale = 1.0 + pulseAmount * sin(uniforms.time * pulseFrequency);

  let offset = uv - uniforms.point;
  var local = offset;
  local.x = local.x * uniforms.aspectRatio;

  let cosTheta = cos(rotation);
  let sinTheta = sin(rotation);
  let rotated = vec2f(
    cosTheta * local.x + sinTheta * local.y,
    -sinTheta * local.x + cosTheta * local.y,
  );

  let effectiveHalfSize = halfSize * max(dynamicScale, 1e-3);
  let normalized = vec2f(rotated.x / effectiveHalfSize.x, rotated.y / effectiveHalfSize.y);
  let distance = max(abs(normalized.x), abs(normalized.y));
  let mask = 1.0 - smoothstep(1.0, 1.0 + softness, distance);

  let col = vec3f(0.0, 0.0, 0.0);
  var delta = vec3f(uniforms.color * mask);

  if (abs(proceduralForce) > 1e-5) {
    let tangential = vec2f(-offset.y, offset.x);
    let tangentialLength = length(tangential);
    if (tangentialLength > 1e-5) {
      let normalizedTangential = tangential / tangentialLength;
      let radiusFactor = clamp(length(normalized), 0.0, 1.0);
      let velocityMagnitude = proceduralForce * radiusFactor * mask;
      let velocityDelta = normalizedTangential * velocityMagnitude;
      delta = vec3f(delta.xy + velocityDelta, delta.z);
    }
  }

  return vec4f(delta, mask);
}

fn computeSplat(uv: vec2f, uniforms: SplatUniforms) -> vec4f {
  switch uniforms.splatType {
    case 0u {
      return gaussianSplat(uv, uniforms);
    }
    case 1u {
      return rectangleSplat(uv, uniforms);
    }
    default {
      return gaussianSplat(uv, uniforms);
    }
  }
}

// Pass 0: Compute the impulse delta for the selected procedural splat
// This is a pure function that computes the splat without reading base
fn pass0(
  uv: vec2f,
  uniforms: SplatUniforms,
  base: texture_2d<f32>,
  baseSampler: sampler,
) -> vec4f {
  let clampedUv = clamp(uv, vec2f(0.0), vec2f(1.0));
  return computeSplat(clampedUv, uniforms);
}

// Pass 1: Apply the delta to the base based on mode
fn pass1(
  uv: vec2f,
  uniforms: SplatUniforms,
  base: texture_2d<f32>,
  baseSampler: sampler,
  pass0Texture: texture_2d<f32>,
  pass0Sampler: sampler,
) -> vec4f {
  let clampedUv = clamp(uv, vec2f(0.0), vec2f(1.0));
  let baseColor = textureSample(base, baseSampler, clampedUv);
  let delta = textureSample(pass0Texture, pass0Sampler, clampedUv);
  
  // Mode-based behavior
  if (uniforms.mode == 0u) {
    // Passthrough mode: just return base
    return baseColor;
  } else if (uniforms.mode == 1u) {
    // Additive mode: add delta * strength to base
    return vec4f(baseColor.xyz + delta.xyz * uniforms.strength, 1.0);
  } else {
    // Reserved for exotic operations (mode >= 2)
    return baseColor;
  }
}

fn load_SplatUniforms() -> SplatUniforms {
  return SplatUniforms(
  uniforms.uniforms_mode,
  uniforms.uniforms_splatType,
  uniforms.uniforms_point,
  uniforms.uniforms_color,
  uniforms.uniforms_radius,
  uniforms.uniforms_aspectRatio,
  uniforms.uniforms_strength,
  uniforms.uniforms_time,
  uniforms.uniforms_shapeParams0,
  uniforms.uniforms_shapeParams1
  );
}

#define CUSTOM_FRAGMENT_DEFINITIONS
@fragment
fn main(input: FragmentInputs) -> FragmentOutputs {
#define CUSTOM_FRAGMENT_MAIN_BEGIN
  let uniforms_value = load_SplatUniforms();
  let uv_local = fragmentInputs.vUV;
  let color = pass1(uv_local, uniforms_value, base, baseSampler, pass0Texture, pass0Sampler);
  fragmentOutputs.color = color;
#define CUSTOM_FRAGMENT_MAIN_END
}

`, // pass1
] as const;
export const SplatUnifiedPassCount = 2 as const;
export const SplatUnifiedPrimaryTextureName = 'base' as const;

export const SplatUnifiedPassTextureSources = [
  [
    { binding: 'base', source: { kind: 'input', key: 'base' } },
  ],
  [
    { binding: 'base', source: { kind: 'input', key: 'base' } },
    { binding: 'pass0Texture', source: { kind: 'pass', passIndex: 0 } },
  ],
] as const;

export const SplatUnifiedUniformMeta: UniformDescriptor[] = [
  {
    name: 'mode',
    kind: 'u32',
    bindingName: 'uniforms_mode',
  },
  {
    name: 'splatType',
    kind: 'u32',
    bindingName: 'uniforms_splatType',
  },
  {
    name: 'point',
    kind: 'vec2f',
    bindingName: 'uniforms_point',
  },
  {
    name: 'color',
    kind: 'vec3f',
    bindingName: 'uniforms_color',
  },
  {
    name: 'radius',
    kind: 'f32',
    bindingName: 'uniforms_radius',
  },
  {
    name: 'aspectRatio',
    kind: 'f32',
    bindingName: 'uniforms_aspectRatio',
  },
  {
    name: 'strength',
    kind: 'f32',
    bindingName: 'uniforms_strength',
  },
  {
    name: 'time',
    kind: 'f32',
    bindingName: 'uniforms_time',
  },
  {
    name: 'shapeParams0',
    kind: 'vec4f',
    bindingName: 'uniforms_shapeParams0',
  },
  {
    name: 'shapeParams1',
    kind: 'vec4f',
    bindingName: 'uniforms_shapeParams1',
  },
];

function ensureVector2(value: BABYLON.Vector2 | readonly [number, number]): BABYLON.Vector2 {
  return value instanceof BABYLON.Vector2 ? value : BABYLON.Vector2.FromArray(value as readonly [number, number]);
}
function ensureVector3(value: BABYLON.Vector3 | readonly [number, number, number]): BABYLON.Vector3 {
  return value instanceof BABYLON.Vector3 ? value : BABYLON.Vector3.FromArray(value as readonly [number, number, number]);
}
function ensureVector4(value: BABYLON.Vector4 | readonly [number, number, number, number]): BABYLON.Vector4 {
  return value instanceof BABYLON.Vector4 ? value : BABYLON.Vector4.FromArray(value as readonly [number, number, number, number]);
}

export interface SplatUnifiedSplatUniforms {
  mode: number;
  splatType: number;
  point: BABYLON.Vector2 | readonly [number, number];
  color: BABYLON.Vector3 | readonly [number, number, number];
  radius: number;
  aspectRatio: number;
  strength: number;
  time: number;
  shapeParams0: BABYLON.Vector4 | readonly [number, number, number, number];
  shapeParams1: BABYLON.Vector4 | readonly [number, number, number, number];
}

export function setSplatUnifiedSplatUniforms(material: BABYLON.ShaderMaterial, uniforms: Partial<SplatUnifiedSplatUniforms>): void {
  if (!uniforms) {
    return;
  }
  if (uniforms.mode !== undefined) {
    material.setUInt('uniforms_mode', uniforms.mode);
  }
  if (uniforms.splatType !== undefined) {
    material.setUInt('uniforms_splatType', uniforms.splatType);
  }
  if (uniforms.point !== undefined) {
    material.setVector2('uniforms_point', ensureVector2(uniforms.point));
  }
  if (uniforms.color !== undefined) {
    material.setVector3('uniforms_color', ensureVector3(uniforms.color));
  }
  if (uniforms.radius !== undefined) {
    material.setFloat('uniforms_radius', uniforms.radius);
  }
  if (uniforms.aspectRatio !== undefined) {
    material.setFloat('uniforms_aspectRatio', uniforms.aspectRatio);
  }
  if (uniforms.strength !== undefined) {
    material.setFloat('uniforms_strength', uniforms.strength);
  }
  if (uniforms.time !== undefined) {
    material.setFloat('uniforms_time', uniforms.time);
  }
  if (uniforms.shapeParams0 !== undefined) {
    material.setVector4('uniforms_shapeParams0', ensureVector4(uniforms.shapeParams0));
  }
  if (uniforms.shapeParams1 !== undefined) {
    material.setVector4('uniforms_shapeParams1', ensureVector4(uniforms.shapeParams1));
  }
}

export type SplatUnifiedTextureName = 'base' | 'pass0Texture';
export interface SplatUnifiedInputs {
  base: ShaderSource;
}

export interface SplatUnifiedMaterialHandles {
  material: BABYLON.ShaderMaterial;
  setTexture(name: SplatUnifiedTextureName, texture: BABYLON.BaseTexture): void;
  setTextureSampler(name: SplatUnifiedTextureName, sampler: BABYLON.TextureSampler): void;
  setUniforms(uniforms: Partial<SplatUnifiedSplatUniforms>): void;
}

export interface SplatUnifiedMaterialOptions {
  name?: string;
  passIndex?: number;
}

export function createSplatUnifiedMaterial(scene: BABYLON.Scene, options: SplatUnifiedMaterialOptions = {}): SplatUnifiedMaterialHandles {
  const passIndex = options.passIndex ?? 0;
  if (passIndex < 0 || passIndex >= 2) {
    throw new Error(`Invalid passIndex ${passIndex} for SplatUnified. Expected 0 <= passIndex < 2.`);
  }
  const baseName = options.name ?? 'SplatUnifiedMaterial';
  const shaderName = `${baseName}_pass${passIndex}`;
  // Register shaders in the WGSL store to enable preprocessor
  const vertexShaderName = `${shaderName}VertexShader`;
  const fragmentShaderName = `${shaderName}FragmentShader`;
  
  BABYLON.ShaderStore.ShadersStoreWGSL[vertexShaderName] = SplatUnifiedVertexSource;
  BABYLON.ShaderStore.ShadersStoreWGSL[fragmentShaderName] = SplatUnifiedFragmentSources[passIndex];
  
  const material = new BABYLON.ShaderMaterial(shaderName, scene, {
    vertex: shaderName,
    fragment: shaderName,
  }, {
    attributes: ['position', 'uv'],
    uniforms: ['uniforms_mode', 'uniforms_splatType', 'uniforms_point', 'uniforms_color', 'uniforms_radius', 'uniforms_aspectRatio', 'uniforms_strength', 'uniforms_time', 'uniforms_shapeParams0', 'uniforms_shapeParams1'],
    samplers: ['base', 'pass0Texture'],
    samplerObjects: ['baseSampler', 'pass0Sampler'],
    shaderLanguage: BABYLON.ShaderLanguage.WGSL,
  });

  const samplerLookup = { 'base': 'baseSampler', 'pass0Texture': 'pass0Sampler' } as const;

  const handles: SplatUnifiedMaterialHandles = {
    material,
    setTexture: (name, texture) => material.setTexture(name, texture),
    setTextureSampler: (name, sampler) => material.setTextureSampler(samplerLookup[name], sampler),
    setUniforms: (values) => setSplatUnifiedSplatUniforms(material, values),
  };

  return handles;
}

export class SplatUnifiedEffect extends CustomShaderEffect<SplatUnifiedSplatUniforms, SplatUnifiedInputs> {
  effectName = 'SplatUnified'

  constructor(engine: BABYLON.WebGPUEngine, inputs: SplatUnifiedInputs, width = 1280, height = 720, sampleMode: 'nearest' | 'linear' = 'linear', precision: RenderPrecision = 'half_float') {
    super(engine, inputs, {
      factory: (sceneRef, options) => createSplatUnifiedMaterial(sceneRef, options),
      textureInputKeys: ['base'],
      textureBindingKeys: ['base', 'pass0Texture'],
      passTextureSources: SplatUnifiedPassTextureSources,
      passCount: 2,
      primaryTextureKey: 'base',
      width,
      height,
      materialName: 'SplatUnifiedMaterial',
      sampleMode,
      precision,
      uniformMeta: SplatUnifiedUniformMeta,
    })
  }

  override setSrcs(inputs: Partial<SplatUnifiedInputs>): void {
    super.setSrcs(inputs);
  }

  override setUniforms(uniforms: { mode?: Dynamic<number>, splatType?: Dynamic<number>, point?: Dynamic<BABYLON.Vector2 | readonly [number, number]>, color?: Dynamic<BABYLON.Vector3 | readonly [number, number, number]>, radius?: Dynamic<number>, aspectRatio?: Dynamic<number>, strength?: Dynamic<number>, time?: Dynamic<number>, shapeParams0?: Dynamic<BABYLON.Vector4 | readonly [number, number, number, number]>, shapeParams1?: Dynamic<BABYLON.Vector4 | readonly [number, number, number, number]> }): void {
    const record: ShaderUniforms = {};
    if (uniforms.mode !== undefined) {
      record['mode'] = uniforms.mode;
    }
    if (uniforms.splatType !== undefined) {
      record['splatType'] = uniforms.splatType;
    }
    if (uniforms.point !== undefined) {
      record['point'] = uniforms.point;
    }
    if (uniforms.color !== undefined) {
      record['color'] = uniforms.color;
    }
    if (uniforms.radius !== undefined) {
      record['radius'] = uniforms.radius;
    }
    if (uniforms.aspectRatio !== undefined) {
      record['aspectRatio'] = uniforms.aspectRatio;
    }
    if (uniforms.strength !== undefined) {
      record['strength'] = uniforms.strength;
    }
    if (uniforms.time !== undefined) {
      record['time'] = uniforms.time;
    }
    if (uniforms.shapeParams0 !== undefined) {
      record['shapeParams0'] = uniforms.shapeParams0;
    }
    if (uniforms.shapeParams1 !== undefined) {
      record['shapeParams1'] = uniforms.shapeParams1;
    }
    super.setUniforms(record);
  }
}

