// Auto-generated by wgsl fragment generator. DO NOT EDIT.
import * as BABYLON from 'babylonjs';
import { CustomShaderEffect, type ShaderSource, type RenderPrecision, type UniformDescriptor, type ShaderUniforms, type Dynamic } from '../shaderFXBabylon';
export const PixelateVertexSource = `// Auto-generated by wgsl fragment generator. DO NOT EDIT.
attribute position: vec3<f32>;
attribute uv: vec2<f32>;
varying vUV: vec2<f32>;

#define CUSTOM_VERTEX_DEFINITIONS
@vertex
fn main(input : VertexInputs) -> FragmentInputs {
#define CUSTOM_VERTEX_MAIN_BEGIN
  vertexOutputs.position = vec4<f32>(vertexInputs.position, 1.0);
  vertexOutputs.vUV = vertexInputs.uv;
#define CUSTOM_VERTEX_MAIN_END
}

`;
export const PixelateFragmentSources = [
  `// Auto-generated by wgsl fragment generator. DO NOT EDIT.
varying vUV: vec2<f32>;
uniform uniforms_pixelSize: f32;
var src: texture_2d<f32>;
var srcSampler: sampler;

// Source fragment function
struct PixelateUniforms {
  pixelSize: f32, // 1.0 min=1.0 max=64.0 step=1.0
};

fn pass0(uv: vec2f, uniforms: PixelateUniforms, src: texture_2d<f32>, srcSampler: sampler) -> vec4f {
  let dims = textureDimensions(src);
  let res = vec2f(
    select(1.0, f32(dims.x), dims.x > 0u),
    select(1.0, f32(dims.y), dims.y > 0u),
  );
  let size = max(uniforms.pixelSize, 1.0);
  let pixelatedUV = floor(uv * res / size) * (size / res);
  return textureSample(src, srcSampler, pixelatedUV);
}

fn load_PixelateUniforms() -> PixelateUniforms {
  return PixelateUniforms(
  uniforms.uniforms_pixelSize
  );
}

#define CUSTOM_FRAGMENT_DEFINITIONS
@fragment
fn main(input: FragmentInputs) -> FragmentOutputs {
#define CUSTOM_FRAGMENT_MAIN_BEGIN
  let uniforms_value = load_PixelateUniforms();
  let uv_local = fragmentInputs.vUV;
  let color = pass0(uv_local, uniforms_value, src, srcSampler);
  fragmentOutputs.color = color;
#define CUSTOM_FRAGMENT_MAIN_END
}

`, // pass0
] as const;
export const PixelatePassCount = 1 as const;
export const PixelatePrimaryTextureName = 'src' as const;

export const PixelatePassTextureSources = [
  [
    { binding: 'src', source: { kind: 'input', key: 'src' } },
  ],
] as const;

export const PixelateUniformMeta: UniformDescriptor[] = [
  {
    name: 'pixelSize',
    kind: 'f32',
    bindingName: 'uniforms_pixelSize',
    default: 1.0,
    ui: { min: 1, max: 64, step: 1 },
  },
];

export interface PixelateUniforms {
  pixelSize: number;
}

export function setPixelateUniforms(material: BABYLON.ShaderMaterial, uniforms: Partial<PixelateUniforms>): void {
  if (!uniforms) {
    return;
  }
  if (uniforms.pixelSize !== undefined) {
    material.setFloat('uniforms_pixelSize', uniforms.pixelSize);
  }
}

export type PixelateTextureName = 'src';
export interface PixelateInputs {
  src: ShaderSource;
}

export interface PixelateMaterialHandles {
  material: BABYLON.ShaderMaterial;
  setTexture(name: PixelateTextureName, texture: BABYLON.BaseTexture): void;
  setTextureSampler(name: PixelateTextureName, sampler: BABYLON.TextureSampler): void;
  setUniforms(uniforms: Partial<PixelateUniforms>): void;
}

export interface PixelateMaterialOptions {
  name?: string;
  passIndex?: number;
}

export function createPixelateMaterial(scene: BABYLON.Scene, options: PixelateMaterialOptions = {}): PixelateMaterialHandles {
  const passIndex = options.passIndex ?? 0;
  if (passIndex < 0 || passIndex >= 1) {
    throw new Error(`Invalid passIndex ${passIndex} for Pixelate. Expected 0 <= passIndex < 1.`);
  }
  const baseName = options.name ?? 'PixelateMaterial';
  const shaderName = `${baseName}_pass${passIndex}`;
  // Register shaders in the WGSL store to enable preprocessor
  const vertexShaderName = `${shaderName}VertexShader`;
  const fragmentShaderName = `${shaderName}FragmentShader`;
  
  BABYLON.ShaderStore.ShadersStoreWGSL[vertexShaderName] = PixelateVertexSource;
  BABYLON.ShaderStore.ShadersStoreWGSL[fragmentShaderName] = PixelateFragmentSources[passIndex];
  
  const material = new BABYLON.ShaderMaterial(shaderName, scene, {
    vertex: shaderName,
    fragment: shaderName,
  }, {
    attributes: ['position', 'uv'],
    uniforms: ['uniforms_pixelSize'],
    samplers: ['src'],
    samplerObjects: ['srcSampler'],
    shaderLanguage: BABYLON.ShaderLanguage.WGSL,
  });

  const samplerLookup = { 'src': 'srcSampler' } as const;

  const handles: PixelateMaterialHandles = {
    material,
    setTexture: (name, texture) => material.setTexture(name, texture),
    setTextureSampler: (name, sampler) => material.setTextureSampler(samplerLookup[name], sampler),
    setUniforms: (values) => setPixelateUniforms(material, values),
  };

  return handles;
}

export class PixelateEffect extends CustomShaderEffect<PixelateUniforms, PixelateInputs> {
  effectName = 'Pixelate'

  constructor(engine: BABYLON.WebGPUEngine, inputs: PixelateInputs, width = 1280, height = 720, sampleMode: 'nearest' | 'linear' = 'linear', precision: RenderPrecision = 'half_float') {
    super(engine, inputs, {
      factory: (sceneRef, options) => createPixelateMaterial(sceneRef, options),
      textureInputKeys: ['src'],
      textureBindingKeys: ['src'],
      passTextureSources: PixelatePassTextureSources,
      passCount: 1,
      primaryTextureKey: 'src',
      width,
      height,
      materialName: 'PixelateMaterial',
      sampleMode,
      precision,
      uniformMeta: PixelateUniformMeta,
    })
    this.setUniforms({
      pixelSize: 1.0,
    });
  }

  override setSrcs(inputs: Partial<PixelateInputs>): void {
    super.setSrcs(inputs);
  }

  override setUniforms(uniforms: { pixelSize?: Dynamic<number> }): void {
    const record: ShaderUniforms = {};
    if (uniforms.pixelSize !== undefined) {
      record['pixelSize'] = uniforms.pixelSize;
    }
    super.setUniforms(record);
  }
}

