// Auto-generated by wgsl fragment generator. DO NOT EDIT.
import * as BABYLON from 'babylonjs';
import { CustomShaderEffect, type ShaderSource, type RenderPrecision, type UniformDescriptor, type ShaderUniforms, type Dynamic } from '../shaderFXBabylon';
export const VerticalBlurVertexSource = `// Auto-generated by wgsl fragment generator. DO NOT EDIT.
attribute position: vec3<f32>;
attribute uv: vec2<f32>;
varying vUV: vec2<f32>;

#define CUSTOM_VERTEX_DEFINITIONS
@vertex
fn main(input : VertexInputs) -> FragmentInputs {
#define CUSTOM_VERTEX_MAIN_BEGIN
  vertexOutputs.position = vec4<f32>(vertexInputs.position, 1.0);
  vertexOutputs.vUV = vertexInputs.uv;
#define CUSTOM_VERTEX_MAIN_END
}

`;
export const VerticalBlurFragmentSources = [
  `// Auto-generated by wgsl fragment generator. DO NOT EDIT.
varying vUV: vec2<f32>;
uniform uniforms_pixels: i32;
uniform uniforms_resolution: f32;
var src: texture_2d<f32>;
var srcSampler: sampler;

// Source fragment function
struct VerticalBlurUniforms {
  pixels: i32,
  resolution: f32,
};

fn pass0(uv: vec2f, uniforms: VerticalBlurUniforms, src: texture_2d<f32>, srcSampler: sampler) -> vec4f {
  // Skip blur if pixels is 0 - just pass through
  if (uniforms.pixels <= 0) {
    return textureSample(src, srcSampler, uv);
  }

  var color = vec4f(0.0);
  var total = 0.0;
  let offset = 1.0 / uniforms.resolution;
  var i: i32 = -uniforms.pixels;
  loop {
    let weight = 1.0 - (abs(f32(i)) / f32(uniforms.pixels + 1));
    let sampleUV = uv + vec2f(0.0, offset * f32(i));
    color = color + textureSample(src, srcSampler, sampleUV) * weight;
    total = total + weight;
    if (i >= uniforms.pixels) {
      break;
    }
    i = i + 1;
  }
  return color / total;
}

fn load_VerticalBlurUniforms() -> VerticalBlurUniforms {
  var result: VerticalBlurUniforms;
  result.pixels = uniforms.uniforms_pixels;
  result.resolution = uniforms.uniforms_resolution;
  return result;
}

#define CUSTOM_FRAGMENT_DEFINITIONS
@fragment
fn main(input: FragmentInputs) -> FragmentOutputs {
#define CUSTOM_FRAGMENT_MAIN_BEGIN
  let uniforms_value = load_VerticalBlurUniforms();
  let uv_local = fragmentInputs.vUV;
  let color = pass0(uv_local, uniforms_value, src, srcSampler);
  fragmentOutputs.color = color;
#define CUSTOM_FRAGMENT_MAIN_END
}

`, // pass0
] as const;
export const VerticalBlurPassCount = 1 as const;
export const VerticalBlurPrimaryTextureName = 'src' as const;

export const VerticalBlurPassTextureSources = [
  [
    { binding: 'src', source: { kind: 'input', key: 'src' } },
  ],
] as const;

export const VerticalBlurUniformMeta: UniformDescriptor[] = [
  {
    name: 'pixels',
    kind: 'i32',
    bindingName: 'uniforms_pixels',
  },
  {
    name: 'resolution',
    kind: 'f32',
    bindingName: 'uniforms_resolution',
  },
];

export interface VerticalBlurUniforms {
  pixels: number;
  resolution: number;
}

export function setVerticalBlurUniforms(material: BABYLON.ShaderMaterial, uniforms: Partial<VerticalBlurUniforms>): void {
  if (!uniforms) {
    return;
  }
  if (uniforms.pixels !== undefined) {
    material.setInt('uniforms_pixels', uniforms.pixels);
  }
  if (uniforms.resolution !== undefined) {
    material.setFloat('uniforms_resolution', uniforms.resolution);
  }
}

export type VerticalBlurTextureName = 'src';
export interface VerticalBlurInputs {
  src: ShaderSource;
}

export interface VerticalBlurMaterialHandles {
  material: BABYLON.ShaderMaterial;
  setTexture(name: VerticalBlurTextureName, texture: BABYLON.BaseTexture): void;
  setTextureSampler(name: VerticalBlurTextureName, sampler: BABYLON.TextureSampler): void;
  setUniforms(uniforms: Partial<VerticalBlurUniforms>): void;
}

export interface VerticalBlurMaterialOptions {
  name?: string;
  passIndex?: number;
}

export function createVerticalBlurMaterial(scene: BABYLON.Scene, options: VerticalBlurMaterialOptions = {}): VerticalBlurMaterialHandles {
  const passIndex = options.passIndex ?? 0;
  if (passIndex < 0 || passIndex >= 1) {
    throw new Error(`Invalid passIndex ${passIndex} for VerticalBlur. Expected 0 <= passIndex < 1.`);
  }
  const baseName = options.name ?? 'VerticalBlurMaterial';
  const shaderName = `${baseName}_pass${passIndex}`;
  // Register shaders in the WGSL store to enable preprocessor
  const vertexShaderName = `${shaderName}VertexShader`;
  const fragmentShaderName = `${shaderName}FragmentShader`;
  
  BABYLON.ShaderStore.ShadersStoreWGSL[vertexShaderName] = VerticalBlurVertexSource;
  BABYLON.ShaderStore.ShadersStoreWGSL[fragmentShaderName] = VerticalBlurFragmentSources[passIndex];
  
  const material = new BABYLON.ShaderMaterial(shaderName, scene, {
    vertex: shaderName,
    fragment: shaderName,
  }, {
    attributes: ['position', 'uv'],
    uniforms: ['uniforms_pixels', 'uniforms_resolution'],
    samplers: ['src'],
    samplerObjects: ['srcSampler'],
    shaderLanguage: BABYLON.ShaderLanguage.WGSL,
  });

  const samplerLookup = { 'src': 'srcSampler' } as const;

  const handles: VerticalBlurMaterialHandles = {
    material,
    setTexture: (name, texture) => material.setTexture(name, texture),
    setTextureSampler: (name, sampler) => material.setTextureSampler(samplerLookup[name], sampler),
    setUniforms: (values) => setVerticalBlurUniforms(material, values),
  };

  return handles;
}

export class VerticalBlurEffect extends CustomShaderEffect<VerticalBlurUniforms, VerticalBlurInputs> {
  effectName = 'VerticalBlur'

  constructor(engine: BABYLON.WebGPUEngine, inputs: VerticalBlurInputs, width = 1280, height = 720, sampleMode: 'nearest' | 'linear' = 'linear', precision: RenderPrecision = 'half_float') {
    super(engine, inputs, {
      factory: (sceneRef, options) => createVerticalBlurMaterial(sceneRef, options),
      textureInputKeys: ['src'],
      textureBindingKeys: ['src'],
      passTextureSources: VerticalBlurPassTextureSources,
      passCount: 1,
      primaryTextureKey: 'src',
      width,
      height,
      materialName: 'VerticalBlurMaterial',
      sampleMode,
      precision,
      uniformMeta: VerticalBlurUniformMeta,
    })
  }

  override setSrcs(inputs: Partial<VerticalBlurInputs>): void {
    super.setSrcs(inputs);
  }

  override setUniforms(uniforms: { pixels?: Dynamic<number>, resolution?: Dynamic<number> }): void {
    const record: ShaderUniforms = {};
    if (uniforms.pixels !== undefined) {
      record['pixels'] = uniforms.pixels;
    }
    if (uniforms.resolution !== undefined) {
      record['resolution'] = uniforms.resolution;
    }
    super.setUniforms(record);
  }
}

